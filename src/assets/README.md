# Assets

Static brand assets for E-Rakshan live in `public/icons/` (favicon, logo).

This folder holds frontend-bundled assets (images imported by components).
The demo build uses inline SVG + CSS visuals, so nothing needs to live here yet —
drop hero imagery, district basemap exports or partner logos here as the project grows.
